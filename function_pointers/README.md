Function Pointers

This project introduces function pointers in C.

Task 0: What's my name

0-print_name.c contains print_name, a function that receives a name and a function pointer. It uses the supplied function to print the name in different formats.

Files

0-print_name.c — Prints a name using a function pointer.

function_pointers.h — Contains function prototypes for the project.
