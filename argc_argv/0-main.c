#include <stdio.h>

/**
 * main - check the code
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("Program name: %s\n", argv[0]);
	return (0);
}
